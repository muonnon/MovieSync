# 🏗️ 시스템 아키텍처

## 개요

MovieSync는 **클라이언트-서버 아키텍처**를 기반으로 하며, TCP 소켓을 통해 실시간 통신을 구현합니다.

## 전체 시스템 구조

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                              MovieSync System                                 │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌───────────────────────┐          ┌───────────────────────────────────┐   │
│  │      Client Layer     │          │          Server Layer              │   │
│  │                       │          │                                    │   │
│  │  ┌─────────────────┐  │   TCP    │  ┌─────────────┐  ┌────────────┐  │   │
│  │  │  LoginFrame     │  │  Socket  │  │   Server    │  │ APIManager │  │   │
│  │  │  (로그인 화면)  │──┼─────────▶│  │  (메인 서버)│  │ (API 연동) │  │   │
│  │  └────────┬────────┘  │   :55555 │  └──────┬──────┘  └─────┬──────┘  │   │
│  │           │           │          │         │                │         │   │
│  │           ▼           │          │         │                │         │   │
│  │  ┌─────────────────┐  │          │  ┌──────▼──────┐         │         │   │
│  │  │   MainFrame     │  │          │  │ Connected   │         │         │   │
│  │  │ (메인 GUI 컨테이너)│◀─────────┼──│   Client    │         │         │   │
│  │  └────────┬────────┘  │          │  │  (스레드)   │         │         │   │
│  │           │           │          │  └──────┬──────┘         │         │   │
│  │   ┌───────┴───────┐   │          │         │                │         │   │
│  │   │               │   │          │         ▼                ▼         │   │
│  │   ▼               ▼   │          │  ┌──────────────────────────────┐  │   │
│  │ Panels          Client│          │  │      DatabaseManager         │  │   │
│  │ (화면들)       (통신) │          │  │      (SQLite 연동)           │  │   │
│  └───────────────────────┘          │  └──────────────────────────────┘  │   │
│                                      └───────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────┘
```

## 계층별 구성요소

### 1. 클라이언트 계층 (Client Layer)

| 컴포넌트 | 파일 | 역할 |
|----------|------|------|
| **LoginFrame** | `LoginFrame.java` | 로그인 화면, 서버 연결 관리 |
| **MainFrame** | `MainFrame.java` | 메인 GUI 컨테이너, 화면 전환 관리 |
| **Client** | `Client.java` | 서버 통신 담당, 메시지 송수신 |
| **CMSGBuilder** | `CMSGBuilder.java` | 클라이언트→서버 메시지 생성 |
| **MovieListPanel** | `MovieListPanel.java` | 영화 목록 표시 |
| **ChatPanel** | `ChatPanel.java` | 채팅방 화면 |
| **ReviewPanel** | `ReviewPanel.java` | 감상평 화면 |
| **BookmarkPanel** | `BookmarkPanel.java` | 북마크 화면 |

### 2. 서버 계층 (Server Layer)

| 컴포넌트 | 파일 | 역할 |
|----------|------|------|
| **Server** | `Server.java` | 메인 서버, 클라이언트 연결 수락 |
| **ConnectedClient** | `Server.java` 내부 | 개별 클라이언트 처리 스레드 |
| **ChatRoom** | `Server.java` 내부 | 채팅방 관리, 브로드캐스트 |
| **DatabaseManager** | `DatabaseManager.java` | SQLite DB 연동 |
| **APIManager** | `APIManager.java` | 영화진흥위원회 API 연동 |
| **MSGBuilder** | `MSGBuilder.java` | 서버→클라이언트 메시지 생성 |
| **ReceivedMSGTokenizer** | `ReceivedMSGTokenizer.java` | 수신 메시지 파싱 |
| **MSGTable** | `MSGTable.java` | 메시지 태그 정의 |

## 데이터 흐름

### 1. 로그인 흐름

```
┌────────┐     ┌────────┐     ┌────────────────┐     ┌────────────────┐
│LoginFrame│───▶│ Client │───▶│ConnectedClient │───▶│DatabaseManager │
└────────┘     └────────┘     └────────────────┘     └────────────────┘
     │              │                │                      │
     │ 1. 닉네임 입력│                │                      │
     │──────────────▶                │                      │
     │              │ 2. LOGIN 메시지 │                      │
     │              │────────────────▶                      │
     │              │                │ 3. 중복 체크         │
     │              │                │─────────────────────▶│
     │              │                │                      │
     │              │                │◀─────────────────────│
     │              │ 4. LOGIN_OK    │   결과 반환          │
     │              │◀───────────────│                      │
     │ 5. MainFrame │                │                      │
     │    전환      │                │                      │
     │◀─────────────│                │                      │
```

### 2. 영화 목록 조회 흐름

```
┌────────────┐     ┌────────┐     ┌────────────────┐     ┌────────────────┐
│MovieListPanel│───▶│ Client │───▶│ConnectedClient │───▶│DatabaseManager │
└────────────┘     └────────┘     └────────────────┘     └────────────────┘
      │                │                  │                      │
      │ 1. loadMovies()│                  │                      │
      │────────────────▶                  │                      │
      │                │ 2. GET_MOVIES    │                      │
      │                │─────────────────▶│                      │
      │                │                  │ 3. getTop10Movies()  │
      │                │                  │─────────────────────▶│
      │                │                  │                      │
      │                │                  │◀─────────────────────│
      │                │ 4. MOVIES_COUNT  │   ResultSet          │
      │                │◀─────────────────│                      │
      │                │ 5. MOVIES_DATA   │                      │
      │                │◀─────────────────│ (반복)               │
      │                │ 6. MOVIES_END    │                      │
      │                │◀─────────────────│                      │
      │ 7. 테이블 갱신 │                  │                      │
      │◀───────────────│                  │                      │
```

### 3. 채팅 메시지 흐름

```
┌──────────┐   ┌────────┐   ┌────────────────┐   ┌──────────┐   ┌────────────────┐
│ChatPanel │──▶│ Client │──▶│ConnectedClient │──▶│ ChatRoom │──▶│ Other Clients  │
└──────────┘   └────────┘   └────────────────┘   └──────────┘   └────────────────┘
     │              │               │                  │               │
     │ 1. sendChat()│               │                  │               │
     │─────────────▶│               │                  │               │
     │              │ 2. CHAT 메시지│                  │               │
     │              │──────────────▶│                  │               │
     │              │               │ 3. broadcast()   │               │
     │              │               │─────────────────▶│               │
     │              │               │                  │ 4. CHAT_ALL   │
     │              │               │                  │──────────────▶│
     │              │◀─────────────────────────────────│               │
     │ 5. 화면 갱신 │               │                  │               │
     │◀─────────────│               │                  │               │
```

## 멀티스레딩 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                         Server                                   │
│                                                                  │
│  ┌─────────────┐                                                │
│  │ Main Thread │ ◀── ServerSocket.accept() 루프                │
│  └──────┬──────┘                                                │
│         │                                                        │
│         │ 클라이언트 연결마다 새 스레드 생성                     │
│         ▼                                                        │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                  ConnectedClient Threads                 │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │    │
│  │  │ Client #1   │  │ Client #2   │  │ Client #N   │      │    │
│  │  │ (user: A)   │  │ (user: B)   │  │ (user: ...)│      │    │
│  │  └─────────────┘  └─────────────┘  └─────────────┘      │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌─────────────┐                                                │
│  │ APIManager  │ ◀── 백그라운드 스레드 (주기적 API 호출)        │
│  │   Thread    │                                                │
│  └─────────────┘                                                │
└─────────────────────────────────────────────────────────────────┘
```

## 동시성 관리

### 공유 자원

| 자원 | 타입 | 용도 |
|------|------|------|
| `clients` | `ArrayList<ConnectedClient>` | 연결된 클라이언트 목록 |
| `clientMap` | `HashMap<String, ConnectedClient>` | 닉네임으로 클라이언트 검색 |
| `chatRooms` | `HashMap<String, ChatRoom>` | 채팅방 관리 |

### 주의사항

현재 구현에서는 기본 `ArrayList`, `HashMap`을 사용하고 있어, 고부하 환경에서는 `ConcurrentHashMap`, `CopyOnWriteArrayList` 등 스레드 안전 컬렉션으로 변경이 권장됩니다.

## 에러 처리 전략

```
┌────────────────┐
│   클라이언트   │
│  IOException   │──▶ server.removeClient() ──▶ 채팅방에서 제거
└────────────────┘                          ──▶ 소켓 종료

┌────────────────┐
│     서버       │
│  SQLException  │──▶ 에러 메시지 반환 ──▶ ERROR//메시지//END
└────────────────┘

┌────────────────┐
│   APIManager   │
│  IOException   │──▶ 로그 출력 ──▶ 다음 스케줄에서 재시도
└────────────────┘
```

---

[← 돌아가기](./README.md)
